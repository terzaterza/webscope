import { SessionComponent } from "../../components/Session";
import { WaveformStream } from "../../core/Stream";

export function SessionTest() {
    return (
        <SessionComponent></SessionComponent>
    );
}